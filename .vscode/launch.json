{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Launch Program",
            "port": 9230,
            "timeout": -1, // Irá manter o debug sempre ligado
            "restart": true, // Quando terminar ele irá reiniciar
            "localRoot": "${workspaceFolder}",
            "remoteRoot": "/usr/app",
            "address": "0.0.0.0"
        }
    ]
}